first_string = input()
second_string = input()
new_word = ""
counter = 0

#for i in range(len(first_string)):

for symbol in second_string:
    g = first_string[counter]
    g = symbol
    print(first_string)
    counter += 1

# for i in range (len(first_string)):
#     for symbol in second_string:
#         first_string[i] = symbol

    #for every_another_letter in range(len(first_string)):
        #second_string[0] = every_letter
        #break